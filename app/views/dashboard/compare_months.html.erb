<%= form_with url: '/dashboard/sales', method: :get do %>
  <%= select_tag 'month', options_for_select(Date::MONTHNAMES.compact.each_with_index.map { |name, i| [name, i+1] }) %>
  <%= submit_tag 'Submit' %>
<% end %>